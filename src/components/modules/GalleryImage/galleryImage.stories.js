import React from "react";
import { storiesOf } from "@storybook/react";
import GalleryImage from "../GalleryImage";

storiesOf("Modules/GalleryImage", module).add("default", () => (
  <GalleryImage />
));
